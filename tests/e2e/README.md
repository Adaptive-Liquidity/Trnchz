# End-to-end tests

Placeholder for **browser** or **Playwright** flows (wallet connect, tx submission). Populate after `apps/web` wallet integration lands.

Critical flows to cover later (from ops docs): connect, auth nonce, join, leave recruiting, live surrender, finalize at three, claim.
