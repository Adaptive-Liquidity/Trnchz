# Integration tests

Placeholder for **Next.js + SDK + local validator** flows. Add a runner (e.g. Vitest + `@solana/web3.js`) in Phase 2+ after instruction surfaces exist.

Do not assert **offchain** winner logic — only observable chain and indexer projections.
